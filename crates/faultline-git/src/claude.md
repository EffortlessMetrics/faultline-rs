# faultline-git Source

Git adapter implementation for history and checkout behavior.

Primary file:
- `crates/faultline-git/src/lib.rs`

Keep command invocation and parsing behavior stable for cross-platform CI.

Validation:
- `cargo test -p faultline-git`

