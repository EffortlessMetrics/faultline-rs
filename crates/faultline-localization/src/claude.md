# faultline-localization Source

Domain engine implementation for localization strategy.

Primary file:
- `crates/faultline-localization/src/lib.rs`

Keep boundary selection and `next_probe` deterministic. Confirm any changes preserve existing test or property outcomes.

Validation:
- `cargo test -p faultline-localization`

