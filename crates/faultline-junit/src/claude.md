# faultline-junit Source

JUnit XML implementation module.

Primary file:
- `crates/faultline-junit/src/lib.rs`

Keep outcome-to-failure mapping consistent with report semantics.

Validation:
- `cargo test -p faultline-junit`

