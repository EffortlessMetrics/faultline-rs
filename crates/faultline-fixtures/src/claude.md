# faultline-fixtures Source

Builders and generators used by tests.

Primary files:
- `crates/faultline-fixtures/src/lib.rs`
- `crates/faultline-fixtures/src/arb.rs`

Keep fixtures deterministic and lightweight; changes ripple to many consuming tests.

Validation:
- `cargo test -p faultline-fixtures`

