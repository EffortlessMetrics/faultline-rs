# faultline-types Source

Shared data-model module for workspace.

Primary file:
- `crates/faultline-types/src/lib.rs`

Preserve public type layout and serde compatibility unless API changes are explicitly requested.

Validation:
- `cargo test -p faultline-types`

