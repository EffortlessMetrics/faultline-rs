# faultline-sarif Source

SARIF export implementation module.

Primary file:
- `crates/faultline-sarif/src/lib.rs`

Keep schema shape and result ordering deterministic for downstream tool ingestion.

Validation:
- `cargo test -p faultline-sarif`

