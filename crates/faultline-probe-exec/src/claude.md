# faultline-probe-exec Source

Probe execution implementation module.

Primary file:
- `crates/faultline-probe-exec/src/lib.rs`

Keep process launch behavior and timeout handling stable unless intentionally adjusting probe semantics.

Validation:
- `cargo test -p faultline-probe-exec`

