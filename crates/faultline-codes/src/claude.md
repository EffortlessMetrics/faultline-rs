# faultline-codes Source

Enumerations and shared diagnostic values.

Primary file:
- `crates/faultline-codes/src/lib.rs`

Keep schema/serde compatibility when changing variants or display strings.

Validation:
- `cargo test -p faultline-codes`

