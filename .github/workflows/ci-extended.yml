name: CI Extended

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always

jobs:
  extended:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - run: cargo install cargo-mutants cargo-deny cargo-audit cargo-semver-checks

      - name: "Contract: mutation testing"
        id: mutants
        run: cargo mutants -- --lib

      - name: "Contract: supply-chain policy (cargo deny)"
        id: deny
        run: cargo deny check

      - name: "Contract: security audit"
        id: audit
        run: cargo audit

      - name: "Contract: semver compatibility"
        id: semver
        run: cargo semver-checks
